import {ApplicationRef, ViewEncapsulation} from '@angular/core';
import {Component} from "../fx/annotations";

@Component({
    selector: 'clock',
})
export class ClockComponent {
    constructor() {
    }
}
